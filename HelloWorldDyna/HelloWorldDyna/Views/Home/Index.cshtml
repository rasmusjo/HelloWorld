
@{
    ViewBag.Title = "Index";
}

@{
    string env = Environment.GetEnvironmentVariable("APPSETTING_SlotName");
}

<h2>Hello World!</h2>
@if (!string.IsNullOrEmpty(env))
{
    <b>@string.Format("We are running in: {0}", env)</b>
}
